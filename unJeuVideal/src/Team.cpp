#include "Team.h"
#include "FactoryCharacter.h"

#include <iostream>
#include <string>
#include <vector>
/*
Team::Team()
{
    //ctor
}

Team::Team(FactoryCharacter character1,FactoryCharacter character2)
{
    team.push_back(character1);
    team.push_back(character2);
}

void Team::affiche()
{
    unsigned int i;

    for(i=0;i<this->getListCharacter().size();i++)
    {
        cout<<"Personnages "<<i+1<<" : "<<this->getListCharacter()[i].getNom<<endl;
    }
}

bool Team::isEmpty()
{
    bool vide=false;

    if(this->getListCharacter().empty())
    {
        vide=true;
    }
    return vide;
}


vector<FactoryCharacter> Team::getListCharacter()
{
    return this->team;
}

void Team::removeCharachter(int n)
{
    this->team.erase(this->team.begin()+n);
}

void Team::AddCharachter(FactoryCharacter p)
{
    this->team.push_back(p);
}

void Team::insertCharacter(FactoryCharacter p, int n)
{
    this->team.insert(this->team.begin()+n,p);
}
*/
